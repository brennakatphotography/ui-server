import React, { Component } from 'react';

import template from '../templates/PublicFolders';

export default class PublicFolders extends Component {
  render() {
    return template(this.props);
  }
}
