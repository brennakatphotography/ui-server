import React, { Component } from 'react';

import template from '../templates/ImageCollectionLink';

export default class ImageCollectionLink extends Component {
  render() {
    return template(this.props);
  }
}
